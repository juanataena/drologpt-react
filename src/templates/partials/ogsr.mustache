{{! ----------------------------------------------------------------------------------}}
{{! ONGIGYASERVICEREADY FUNCTION TEMPLATE }}
{{! }}
{{! logEnabled is a boolean that can be enabled disabled at real time using the declared constant }}
{{! }}
{{! }}
{{! ----------------------------------------------------------------------------------}}
{{#onGigyaServiceReady}}
    onGigyaServiceReady: function(serviceName) {

        {{! OLD CONTENT }}
        {{#value}}
            {{{value}}}
        {{/value}}

        {{! GENERATED CONTENT & COMMENTS SECTION }}
        {{#valueTemplate}}
        // [ BEGIN '{{templateName}}' ] - Import file section automatically generated on {{date}}
            {{{valueTemplate}}}
        // [ END '{{templateName}}' ] - Import file section automatically generated on {{date}}
        {{/valueTemplate}}
    }
{{/onGigyaServiceReady}}
{{! ----------------------------------------------------------------------------------}}
{{! END LOGGING FUNCTION TEMPLATE }}
{{! ----------------------------------------------------------------------------------}}
